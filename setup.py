#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='ActiveNoiseHoomd',
    version='0.1.0',
    packages=find_packages(include=['ActiveNoiseHoomd', 'ActiveNoiseHoomd.*'])
)
